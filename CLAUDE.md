# CLAUDE.md

## Workflow
- Commit and push after each change.
- Push protocol: `git push origin main`
- Always bump `NIGHT_SHIFT_VERSION` in `skill/SKILL.md` when making changes to it. Increment the letter suffix (e.g. `2026-04-12a` → `2026-04-12b`), or use today's date with suffix `a` if the date changed.
